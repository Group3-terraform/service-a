apiVersion: v1
kind: Service
metadata:
  name: service-a
  namespace: apps
spec:
  selector:
    app: service-a
  ports:
    - name: http
      port: 8080       # service port
      targetPort: 8080 # pod port
  type: ClusterIP
